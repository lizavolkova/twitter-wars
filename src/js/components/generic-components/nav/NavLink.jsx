import React from 'react';

class NavLink extends React.Component {

    /**
     * Render component
     * @returns {XML}
     */
    render() {

        return (
            <a href="#">LINK</a>
        )
    }
}

export default NavLink;